{
  "name": "ai-english-backend",
  "runtime": "python",
  "buildCommand": "pip install -r requirements.txt",
  "startCommand": "python main.py",
  "envVars": {
    "DASHSCOPE_API_KEY": {
      "sync": false
    }
  }
}